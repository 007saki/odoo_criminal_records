<odoo>

    <!-- Top-level menu -->
    <menuitem id="menu_criminal_root" name="Criminal Records"/>

    <!-- Action -->
    <record id="action_criminal_record" model="ir.actions.act_window">
        <field name="name">Criminal Records</field>
        <field name="res_model">criminal.record</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Menu Item -->
    <menuitem id="menu_criminal_record"
              name="Criminal Records"
              parent="menu_criminal_root"
              action="action_criminal_record"/>

</odoo>
